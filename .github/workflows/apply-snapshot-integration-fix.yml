name: Apply snapshot integration fix

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.ref == 'feature/rep-system-v2-mainline'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/rep-system-v2-mainline
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Apply integration correction
        run: |
          if [ -f .final-review/integration_snapshot_fix.py ]; then
            python3 .final-review/integration_snapshot_fix.py
          else
            echo "Integration correction is already published."
          fi

      - name: Verify
        run: mvn --batch-mode --no-transfer-progress clean verify

      - name: Publish verified correction
        run: |
          rm -f .final-review/integration_snapshot_fix.py
          if git diff --quiet -- \
            .final-review/integration_snapshot_fix.py \
            src/main/java/org/enthusia/rep/CommendPlugin.java \
            src/main/java/org/enthusia/rep/storage/OrderedSnapshotWriter.java \
            src/test/java/org/enthusia/rep/storage/OrderedSnapshotWriterTest.java; then
            echo "No source changes to publish."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A \
            .final-review \
            src/main/java/org/enthusia/rep/CommendPlugin.java \
            src/main/java/org/enthusia/rep/storage/OrderedSnapshotWriter.java \
            src/test/java/org/enthusia/rep/storage/OrderedSnapshotWriterTest.java
          git commit -m "fix: retain cooldowns in ordered composite snapshots"
          git push origin HEAD:feature/rep-system-v2-mainline
